{ ... }:

{
  system.defaults.finder = {
    # 現在デフォルトから変更している設定なし
    # 必要に応じて以下の設定を有効化:
    # ShowPathbar = true;              # パスバーを表示
    # AppleShowAllExtensions = true;   # すべてのファイル名拡張子を表示
    # FXPreferredViewStyle = "Nlsv";   # リスト表示をデフォルトに
  };
}
