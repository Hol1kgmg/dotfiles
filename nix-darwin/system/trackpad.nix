{ ... }:

{
  system.defaults.trackpad = {
    # 現在デフォルトから変更している設定なし
    # 必要に応じて以下の設定を有効化:
    # Clicking = true;                  # タップでクリック
    # TrackpadThreeFingerDrag = true;   # 3本指ドラッグ
  };
}
